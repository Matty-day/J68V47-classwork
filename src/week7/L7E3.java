package week7;
import java.util.*;

public class L7E3 {
    public static void main(String[] args) {
        int score = 0
        int lives = 3
        int useranwser = 0
        int anwser = 0
        int count = 1
        while count <= 10 + lives > 0
        do {
            anwser = count + (count*count);
            if anwser = useranwser
                    score = correctAnswer(score);
            else
                lives = wrongAnwser(score);
        }while count = count + 1;
    }
}
